# ===========================================
# Configuration Gmail OAuth 2.0
# ===========================================
# Pour obtenir ces valeurs, exécutez:
#   node scripts/setup-gmail-oauth.js
#
# Ou suivez les étapes manuellement:
# 1. Créer un projet sur https://console.cloud.google.com
# 2. Activer l'API Gmail
# 3. Créer des identifiants OAuth 2.0 (Application de bureau)
# 4. Utiliser le script setup-gmail-oauth.js pour obtenir le refresh token

# Client ID de l'application OAuth (obligatoire)
GMAIL_CLIENT_ID=your_client_id_here

# Client Secret de l'application OAuth (obligatoire)
GMAIL_CLIENT_SECRET=your_client_secret_here

# URI de redirection OAuth (ne pas modifier)
GMAIL_REDIRECT_URI=urn:ietf:wg:oauth:2.0:oob

# Token de rafraîchissement OAuth (obligatoire)
# Obtenu via le script setup-gmail-oauth.js
GMAIL_REFRESH_TOKEN=your_refresh_token_here

# Adresse email du compte Gmail à surveiller (obligatoire)
GMAIL_USER_EMAIL=your_email@gmail.com

# ===========================================
# Configuration optionnelle
# ===========================================

# ID du label à surveiller (par défaut: tous les messages)
# Exemples: INBOX, UNREAD, SPAM, etc.
# GMAIL_LABEL_ID=INBOX

# Requête de recherche Gmail (par défaut: in:inbox is:unread)
# Syntaxe: https://support.google.com/mail/answer/7190
# Exemples:
#   - in:inbox is:unread : Messages non lus dans la boîte de réception
#   - from:someone@example.com : Messages d'un expéditeur spécifique
#   - subject:urgent : Messages avec "urgent" dans l'objet
# GMAIL_QUERY=in:inbox is:unread

# Nombre maximum de messages à récupérer par synchronisation
# Valeur par défaut: 10
# GMAIL_MAX_RESULTS=10

# ===========================================
# Configuration de la base de données
# ===========================================
# Décommentez et configurez selon votre base de données

# DATABASE_URL=mysql://user:password@localhost:3306/database_name
